name: Build Apps

on:
  push:
    branches:
      - master
      - main
  pull_request:
    branches:
      - master
      - main
  workflow_dispatch:

permissions:
  contents: read
  checks: write

jobs:
  build-android-debug:
    name: Build Android App (Debug)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./android

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build Android app (debug)
        run: ./gradlew :app:assembleDebug --no-daemon --stacktrace

      - name: Upload Android APK (debug)
        uses: actions/upload-artifact@v7
        with:
          name: android-app-debug
          path: android/app/build/outputs/apk/debug/*.apk
          retention-days: 30
          archive: false

  build-android-release:
    name: Build Android App (Release)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./android

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build Android app (release)
        run: ./gradlew :app:assembleRelease --no-daemon --stacktrace

      - name: Upload Android APK (release)
        uses: actions/upload-artifact@v7
        with:
          name: android-app-release
          path: android/app/build/outputs/apk/release/*.apk
          retention-days: 30
          archive: false

  test-android:
    name: Test Android App
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./android

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Run Android unit tests (debug)
        run: ./gradlew :app:testDebugUnitTest --no-daemon --stacktrace

      - name: Upload Android test results
        uses: actions/upload-artifact@v7
        if: always()
        with:
          name: android-test-results
          path: |
            android/app/build/test-results/testDebugUnitTest/*.xml
            android/app/build/reports/tests/testDebugUnitTest/
          retention-days: 30

      - name: Publish Android test report
        uses: dorny/test-reporter@v2
        if: always()
        with:
          name: Android Unit Tests
          path: android/app/build/test-results/testDebugUnitTest/*.xml
          reporter: java-junit
          fail-on-error: true

  build-wear-debug:
    name: Build Wear OS App (Debug)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./android

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build Wear OS app (debug)
        run: ./gradlew :wear:assembleDebug --no-daemon --stacktrace

      - name: Upload Wear OS APK (debug)
        uses: actions/upload-artifact@v7
        with:
          name: wear-app-debug
          path: android/wear/build/outputs/apk/debug/*.apk
          retention-days: 30
          archive: false

  build-wear-release:
    name: Build Wear OS App (Release)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./android

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build Wear OS app (release)
        run: ./gradlew :wear:assembleRelease --no-daemon --stacktrace

      - name: Upload Wear OS APK (release)
        uses: actions/upload-artifact@v7
        with:
          name: wear-app-release
          path: android/wear/build/outputs/apk/release/*.apk
          retention-days: 30
          archive: false

  validate-pwa:
    name: Validate PWA
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Validate PWA files exist
        run: |
          echo "Checking PWA files..."

          if [ ! -f "pwa/index.html" ]; then
            echo "Error: pwa/index.html not found"
            exit 1
          fi

          if [ ! -f "pwa/manifest.json" ]; then
            echo "Error: pwa/manifest.json not found"
            exit 1
          fi

          if [ ! -f "pwa/sw.js" ]; then
            echo "Error: pwa/sw.js not found"
            exit 1
          fi

          echo "All required PWA files exist ✓"

      - name: Validate manifest.json
        run: |
          echo "Validating manifest.json..."

          if ! command -v jq &> /dev/null; then
            echo "Installing jq..."
            sudo apt-get update && sudo apt-get install -y jq
          fi

          if ! jq empty pwa/manifest.json; then
            echo "Error: manifest.json is not valid JSON"
            exit 1
          fi

          echo "manifest.json is valid JSON ✓"

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '20.x'

      - name: Validate service worker syntax
        run: |
          echo "Validating service worker JavaScript syntax..."

          # Check for syntax errors
          node --check pwa/sw.js

          echo "Service worker syntax is valid ✓"

      - name: Validate index.html
        run: |
          echo "Checking index.html structure..."

          # Check if critical elements exist
          if ! grep -q "<html" pwa/index.html; then
            echo "Error: No <html> tag found"
            exit 1
          fi

          if ! grep -q "manifest.json" pwa/index.html; then
            echo "Error: No reference to manifest.json found"
            exit 1
          fi

          echo "index.html structure is valid ✓"

      - name: Upload PWA artifact
        uses: actions/upload-artifact@v7
        with:
          name: pwa
          path: pwa/
          retention-days: 30

  test-pwa:
    name: Test PWA
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./pwa

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '20.x'
          cache: 'npm'
          cache-dependency-path: pwa/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test

      - name: Generate coverage report
        run: npm run test:coverage

      - name: Upload coverage report
        uses: actions/upload-artifact@v7
        with:
          name: pwa-coverage
          path: pwa/coverage/
          retention-days: 30

      - name: Display coverage summary
        run: |
          echo "Test Coverage Summary:"
          echo "====================="
          if [ -f "coverage/coverage-summary.json" ]; then
            node -e "
              const coverage = require('./coverage/coverage-summary.json');
              const total = coverage.total;
              console.log('Lines:      ' + total.lines.pct + '%');
              console.log('Statements: ' + total.statements.pct + '%');
              console.log('Functions:  ' + total.functions.pct + '%');
              console.log('Branches:   ' + total.branches.pct + '%');
            "
          else
            echo "Coverage summary not found"
          fi

  build-summary:
    name: Build Summary
    runs-on: ubuntu-latest
    needs: [build-android-debug, build-android-release, test-android, build-wear-debug, build-wear-release, validate-pwa, test-pwa]
    if: always()

    steps:
      - name: Check build results
        run: |
          echo "Build Summary:"
          echo "=============="
          echo "Android App Build (Debug): ${{ needs.build-android-debug.result }}"
          echo "Android App Build (Release): ${{ needs.build-android-release.result }}"
          echo "Android App Tests: ${{ needs.test-android.result }}"
          echo "Wear OS App (Debug): ${{ needs.build-wear-debug.result }}"
          echo "Wear OS App (Release): ${{ needs.build-wear-release.result }}"
          echo "PWA Validation: ${{ needs.validate-pwa.result }}"
          echo "PWA Tests: ${{ needs.test-pwa.result }}"

          if [ "${{ needs.build-android-debug.result }}" != "success" ] || \
             [ "${{ needs.build-android-release.result }}" != "success" ] || \
             [ "${{ needs.test-android.result }}" != "success" ] || \
             [ "${{ needs.build-wear-debug.result }}" != "success" ] || \
             [ "${{ needs.build-wear-release.result }}" != "success" ] || \
             [ "${{ needs.validate-pwa.result }}" != "success" ] || \
             [ "${{ needs.test-pwa.result }}" != "success" ]; then
            echo ""
            echo "❌ One or more jobs failed"
            exit 1
          fi

          echo ""
          echo "✅ All jobs successful!"
